

## Deploy klaviyo-profile-events Edge Function

The `klaviyo-profile-events` edge function already exists at `supabase/functions/klaviyo-profile-events/index.ts` and the `KLAVIYO_PRIVATE_KEY` secret is already configured.

### Steps
1. Deploy the `klaviyo-profile-events` edge function
2. Test it with a sample request to verify it responds correctly

No code changes needed — just deployment.

